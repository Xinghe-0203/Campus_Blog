# 外部服务需求文档

## 概述

本文档整理校园博客论坛系统所需的外部服务，帮助开发者了解各服务的用途、推荐方案及配置方法。

---

## 1. 邮件服务（必需 - 密码找回功能）

### 需求背景

当前系统具备用户注册和登录功能，但缺少**密码找回**功能。当用户忘记密码时，需要通过邮件验证码方式重置密码，因此必须接入邮件服务。

### 推荐方案

| 服务商 | 免费额度 | 特点 | 推荐指数 |
|--------|----------|------|----------|
| **SendGrid** | 每月 100 封（免费版） | API 友好，文档完善，全球覆盖 | ⭐⭐⭐⭐⭐ |
| **Amazon SES** | 每月 6.2 万封（新用户） | AWS 生态集成，成本低 | ⭐⭐⭐⭐ |
| **Mailgun** | 每月 5 千封 | 开发者友好，有 SMTP 支持 | ⭐⭐⭐⭐ |
| **阿里云邮件推送** | 每月 2 千封 | 国内访问快，无需备案 | ⭐⭐⭐ |
| **QQ 邮箱 / 163 邮箱** | 有限 | 需开启 SMTP，稳定性一般 | ⭐⭐ |

**推荐首选 SendGrid**，免费额度充足，API 简洁，与 Spring Boot 集成方便。

### 配置步骤

#### 1. 注册 SendGrid 账号

1. 访问 https://signup.sendgrid.com
2. 选择免费计划（Flexible Plan）
3. 验证邮箱后登录

#### 2. 创建 API Key

1. 登录后进入 Settings → API Keys
2. 点击 Create API Key
3. 选择 **Full Access** 或 **Restricted Access**（仅需邮件发送权限）
4. 复制生成的 API Key（只显示一次，请妥善保存）

#### 3. 验证发件人域名（可选）

免费版需验证发件人邮箱，付费版可使用任意发件人。

### .env 配置示例

```env
# ==================== 邮件服务配置 ====================
MAIL_HOST=smtp.sendgrid.net
MAIL_PORT=587
MAIL_USERNAME=apikey
MAIL_PASSWORD=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAIL_FROM=noreply@campus-blog.com
```

### Spring Boot 集成方式

在 `pom.xml` 中添加依赖：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-mail</artifactId>
</dependency>
```

在 `application.yml` 中配置：

```yaml
spring:
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
```

### 代码实现要点

```java
// 邮件服务接口示例
public interface EmailService {
    void sendVerificationCode(String to, String code);
    void sendPasswordResetEmail(String to, String token);
}
```

---

## 2. Redis（生产环境推荐）

### 需求背景

当前系统使用 **Caffeine 本地缓存**，适合单机部署。生产环境建议使用 **Redis 分布式缓存**，支持：

- 分布式缓存（多实例共享）
- 分布式锁（解决并发问题）
- Session 共享（多节点部署）
- 消息队列（异步任务）

### 服务商选择

| 服务商 | 免费额度 | 特点 | 推荐指数 |
|--------|----------|------|----------|
| **Redis Cloud** | 30MB 免费 | 官方托管，易用性强 | ⭐⭐⭐⭐⭐ |
| **Amazon ElastiCache** | 750 小时/月 | AWS 生态集成 | ⭐⭐⭐⭐ |
| **阿里云 Redis** | 1GB/月 | 国内访问快 | ⭐⭐⭐⭐ |
| **腾讯云 Redis** | 1GB/月 | 国内访问快 | ⭐⭐⭐ |

### 当前状态

系统已集成 Caffeine 本地缓存：

```yaml
cache:
  type: caffeine
  caffeine:
    spec: maximumSize=1000,expireAfterWrite=300s
```

### 迁移到 Redis 的配置示例

#### .env 配置

```env
# ==================== Redis 配置 ====================
REDIS_HOST=your-redis-host.redis.cloud
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
```

#### application.yml 配置

```yaml
spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      timeout: 6000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
```

#### 缓存配置类

```java
@Configuration
@EnableCaching
public class RedisConfig extends CachingConfigurerSupport {
    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);
        template.setKeySerializer(new StringRedisSerializer());
        template.setValueSerializer(new GenericJackson2JsonRedisSerializer());
        return template;
    }
}
```

### 优先级

- **开发/测试环境**：继续使用 Caffeine（无需额外配置）
- **生产环境**：建议接入 Redis（至少 1GB 规格）

---

## 3. CDN（可选 - 部署加速）

### 用途

- 静态资源加速（CSS、JS、图片）
- 视频内容分发
- 减轻服务器带宽压力

### 服务商选择

| 服务商 | 免费额度 | 特点 | 推荐指数 |
|--------|----------|------|----------|
| **Cloudflare** | 无限次请求 | 全球 CDN， DDoS 防护 | ⭐⭐⭐⭐⭐ |
| **阿里云 CDN** | 每月 10GB | 国内加速，覆盖广 | ⭐⭐⭐⭐ |
| **腾讯云 CDN** | 每月 10GB | 国内加速 | ⭐⭐⭐⭐ |
| **七牛云 CDN** | 每月 10GB | 开发者友好 | ⭐⭐⭐ |

### 配置要点

1. 将静态资源（`/uploads`、`/static`）通过 CDN 加速
2. 配置缓存策略（图片 1 个月，CSS/JS 1 周）
3. 设置 CDN 刷新机制（内容更新时自动刷新）

---

## 4. 对象存储（可选 - 媒体文件存储）

### 用途

用户上传的图片、视频等媒体文件存储。当前系统使用**本地存储**（`uploads` 目录），对象存储可提供：

- 更高的可靠性（99.999999999%）
- 更好的扩展性
- CDN 集成更方便
- 减轻服务器存储压力

### 服务商选择

| 服务商 | 免费额度 | 特点 | 推荐指数 |
|--------|----------|------|----------|
| **阿里云 OSS** | 40GB/月 | 国内访问快，生态完善 | ⭐⭐⭐⭐⭐ |
| **腾讯云 COS** | 50GB/月 | 国内访问快 | ⭐⭐⭐⭐ |
| **AWS S3** | 5GB/月 | 全球覆盖，生态完善 | ⭐⭐⭐⭐ |
| **七牛云** | 10GB/月 | 开发者友好 | ⭐⭐⭐ |
| **本地存储** | 无限 | 简单，无需额外配置 | ⭐⭐⭐ |

### 当前状态

系统已实现本地文件存储：

```yaml
upload:
  base-path: uploads
  image:
    max-width: 1920
    quality: 0.85
```

### 迁移到 OSS 的配置示例

#### .env 配置

```env
# ==================== 阿里云 OSS 配置 ====================
ALIYUN_OSS_ENDPOINT=oss-cn-shanghai.aliyuncs.com
ALIYUN_OSS_ACCESS_KEY_ID=your_access_key_id
ALIYUN_OSS_ACCESS_KEY_SECRET=your_access_key_secret
ALIYUN_OSS_BUCKET_NAME=campus-blog-media
```

#### Spring Boot 依赖

```xml
<dependency>
    <groupId>com.aliyun.oss</groupId>
    <artifactId>aliyun-sdk-oss</artifactId>
    <version>3.17.4</version>
</dependency>
```

#### 代码实现要点

```java
@Service
public class AliyunOssService {
    // 上传文件到 OSS
    public String uploadFile(MultipartFile file, String dir) {
        // 生成文件路径
        String fileName = dir + "/" + UUID.randomUUID() + "-" + file.getOriginalFilename();
        // 上传到 OSS
        ossClient.putObject(bucketName, fileName, file.getInputStream());
        // 返回访问 URL
        return "https://" + bucketName + "." + endpoint + "/" + fileName;
    }
}
```

### 优先级

- **当前阶段**：继续使用本地存储（功能完整，满足需求）
- **后续扩展**：可迁移到阿里云 OSS 或腾讯云 COS

---

## 服务优先级总览

| 服务 | 优先级 | 原因 | 当前状态 |
|------|--------|------|----------|
| **邮件服务** | ⭐⭐⭐⭐⭐ 高 | 密码找回功能必需 | ✅ 已实现（需要配置邮件服务器） |
| **Redis** | ⭐⭐⭐ 中 | 生产环境推荐，开发环境可用 Caffeine 替代 | 已使用 Caffeine |
| **对象存储** | ⭐⭐ 低 | 可选，本地存储已满足需求 | 本地存储 |
| **CDN** | ⭐⭐ 低 | 可选，初期访问量低无需配置 | 未使用 |

---

## 快速开始配置

邮件服务已实现，只需配置邮件服务器即可启用：

1. 注册 SendGrid 账号（https://signup.sendgrid.com）或使用其他 SMTP 服务
2. 创建 API Key 或获取 SMTP 凭证
3. 在 `.env` 文件中添加邮件配置（参考下方配置示例）
4. 确保 `spring-boot-starter-mail` 依赖已在 `pom.xml` 中（已添加）
5. 重启应用即可

---

## 参考资料

- SendGrid 文档：https://docs.sendgrid.com
- Spring Boot Mail：https://docs.spring.io/spring-boot/docs/current/reference/html/io.html#io.email
- Redis 官方文档：https://redis.io/documentation
- 阿里云 OSS 文档：https://help.aliyun.com/document_detail/31883.html
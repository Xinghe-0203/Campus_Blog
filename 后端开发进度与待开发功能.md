# 校园博客论坛系统 - 后端开发进度与待开发功能

---

## 📊 整体开发进度总览

| 模块 | 状态 | 完成度 |
| :--- | :--- | :--- |
| 📊 数据库设计 | ✅ 已完成 | 100% |
| ⚙️ 后端项目骨架 | ✅ 已完成 | 100% |
| 🛠️ 后端基础完善 | ✅ 已完成 | 100% |
| ✅ 版本兼容性修复 | ✅ 已完成 | 100% |
| 🔐 用户认证模块 | ⏳ 待开发 | 0% |
| 📝 文章管理模块 | ⏳ 待开发 | 0% |
| 💬 评论互动模块 | ⏳ 待开发 | 0% |
| ❤️ 点赞收藏模块 | ⏳ 待开发 | 0% |
| 🏷️ 标签管理模块 | ⏳ 待开发 | 0% |
| 🔒 Spring Security + JWT | ⏳ 待开发 | 0% |

---

## ✅ 已完成的功能模块

### 1. 数据库层
- ✅ 7 张核心数据表设计
- ✅ 完整 SQL 初始化脚本
- ✅ 示例数据（管理员账号、标签）
- ✅ 逻辑删除支持
- ✅ 自动时间戳支持

### 2. 实体层 (Entity)
- ✅ SysUser - 用户实体
- ✅ BlogPost - 文章实体
- ✅ BlogComment - 评论实体
- ✅ BlogTag - 标签实体
- ✅ BlogPostTag - 文章-标签关联实体
- ✅ BlogLike - 点赞记录实体
- ✅ BlogCollect - 收藏记录实体

### 3. 持久层 (Mapper)
- ✅ SysUserMapper
- ✅ BlogPostMapper
- ✅ BlogCommentMapper
- ✅ BlogTagMapper
- ✅ BlogPostTagMapper
- ✅ BlogLikeMapper
- ✅ BlogCollectMapper

### 4. 业务层 (Service)
- ✅ SysUserService + 实现
- ✅ BlogPostService + 实现
- ✅ BlogCommentService + 实现
- ✅ BlogTagService + 实现
- ✅ BlogPostTagService + 实现
- ✅ BlogLikeService + 实现
- ✅ BlogCollectService + 实现

### 5. 公共层 (Common)
- ✅ Result<T> 统一响应封装
- ✅ BusinessException 业务异常
- ✅ GlobalExceptionHandler 全局异常处理

### 6. 配置层 (Config)
- ✅ MyMetaObjectHandler 自动填充处理器
- ✅ MybatisPlusConfig MyBatis Plus 配置
- ✅ SecurityConfig Spring Security 配置（待启用）

### 7. 控制层 (Controller)
- ✅ SysUserController 用户控制器
  - ✅ GET /api/user/list - 查询所有用户
  - ✅ GET /api/user/{id} - 根据ID查询用户

---

## ⏳ 待开发功能详细清单

---

## 🔐 模块一：用户认证模块（优先开发）

### 1.1 用户注册
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 用户注册 | POST | `/api/user/register` | P0 🔥 |

**请求参数**：
```json
{
  "username": "用户名",
  "password": "密码",
  "nickname": "昵称",
  "email": "邮箱"
}
```

**实现要点**：
- 检查用户名是否已存在
- 密码使用 BCrypt 加密
- 默认角色为 user
- 默认状态为 1（正常）
- 自动填充 createTime 和 updateTime

---

### 1.2 用户登录
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 用户登录 | POST | `/api/user/login` | P0 🔥 |

**请求参数**：
```json
{
  "username": "用户名",
  "password": "密码"
}
```

**响应数据**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "id": 1,
    "username": "admin",
    "nickname": "系统管理员",
    "role": "admin",
    "token": "jwt_token_here"
  },
  "timestamp": 1234567890123
}
```

**实现要点**：
- 验证用户名是否存在
- 使用 BCrypt 验证密码
- 验证账号状态是否正常
- 后续版本：生成并返回 JWT Token

---

### 1.3 获取当前登录用户信息
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取当前用户信息 | GET | `/api/user/info` | P1 ⭐ |

**实现要点**：
- 从 Token 或 Session 中获取当前用户ID
- 查询用户信息并返回
- 不返回密码字段

---

### 1.4 修改用户信息
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 修改用户信息 | PUT | `/api/user/info` | P1 ⭐ |

**请求参数**：
```json
{
  "nickname": "新昵称",
  "email": "新邮箱",
  "avatar": "头像URL"
}
```

**实现要点**：
- 只能修改自己的信息
- 验证用户是否登录
- 不能修改用户名、密码、角色
- 自动更新 updateTime

---

### 1.5 修改密码（可选）
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 修改密码 | PUT | `/api/user/password` | P2 📅 |

---

## 📝 模块二：文章管理模块

### 2.1 发布文章
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 发布文章 | POST | `/api/post` | P0 🔥 |

**请求参数**：
```json
{
  "title": "文章标题",
  "summary": "文章摘要",
  "content": "文章内容（Markdown）",
  "category": "分类",
  "tagIds": [1, 2, 3]
}
```

**实现要点**：
- 验证用户是否登录
- 设置文章作者为当前用户
- 设置文章状态为 1（已发布）
- 初始化阅读量、点赞数、评论数为0
- 保存文章标签关联（blog_post_tag）
- 自动填充 createTime 和 updateTime

---

### 2.2 获取文章详情
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取文章详情 | GET | `/api/post/{id}` | P0 🔥 |

**实现要点**：
- 根据ID查询文章
- 阅读量 +1
- 关联查询作者信息
- 关联查询标签列表
- 不返回逻辑删除的文章

---

### 2.3 获取文章列表（分页）
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取文章列表 | GET | `/api/post/list` | P0 🔥 |

**请求参数**：
```
page: 页码（默认1）
size: 每页数量（默认10）
category: 分类（可选）
keyword: 关键词（可选）
```

**响应数据**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "文章标题",
        "summary": "摘要",
        "category": "分类",
        "viewCount": 100,
        "likeCount": 10,
        "commentCount": 5,
        "username": "作者",
        "createTime": "2026-04-24 20:00:00"
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

**实现要点**：
- 支持分页查询
- 支持按分类筛选
- 支持按关键词搜索（标题、摘要）
- 只查询已发布且未删除的文章
- 按创建时间倒序排列

---

### 2.4 更新文章
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 更新文章 | PUT | `/api/post/{id}` | P1 ⭐ |

**实现要点**：
- 验证用户是否登录
- 验证用户是否为文章作者或管理员
- 只能修改标题、摘要、内容、分类、标签
- 自动更新 updateTime

---

### 2.5 删除文章
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 删除文章 | DELETE | `/api/post/{id}` | P1 ⭐ |

**实现要点**：
- 验证用户是否登录
- 验证用户是否为文章作者或管理员
- 使用逻辑删除（is_deleted = 1）

---

### 2.6 获取我的文章列表
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取我的文章 | GET | `/api/post/my` | P1 ⭐ |

---

## 💬 模块三：评论互动模块

### 3.1 发表评论
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 发表评论 | POST | `/api/comment` | P0 🔥 |

**请求参数**：
```json
{
  "postId": 1,
  "parentId": null,
  "content": "评论内容"
}
```

**实现要点**：
- 验证用户是否登录
- 验证文章是否存在
- parentId 为空表示一级评论
- 文章评论数 +1
- 自动填充 createTime

---

### 3.2 获取文章评论列表
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取文章评论 | GET | `/api/comment/post/{postId}` | P0 🔥 |

**响应数据**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "postId": 1,
      "userId": 1,
      "username": "评论者",
      "parentId": null,
      "content": "评论内容",
      "createTime": "2026-04-24 20:00:00",
      "replies": [
        {
          "id": 2,
          "parentId": 1,
          "content": "回复内容",
          "username": "回复者",
          "createTime": "2026-04-24 21:00:00"
        }
      ]
    }
  ]
}
```

**实现要点**：
- 查询文章的所有一级评论
- 查询每条一级评论的回复（二级评论）
- 关联查询评论者信息
- 按创建时间正序排列

---

### 3.3 删除评论
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 删除评论 | DELETE | `/api/comment/{id}` | P1 ⭐ |

**实现要点**：
- 验证用户是否登录
- 验证用户是否为评论作者或管理员
- 使用逻辑删除
- 文章评论数 -1

---

## ❤️ 模块四：点赞收藏模块

### 4.1 点赞/取消点赞
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 点赞/取消点赞 | POST | `/api/like/{postId}` | P0 🔥 |

**实现要点**：
- 验证用户是否登录
- 检查是否已点赞
- 已点赞则取消点赞，文章点赞数 -1
- 未点赞则点赞，文章点赞数 +1
- 返回当前状态（是否已点赞）

---

### 4.2 检查是否已点赞
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 检查是否已点赞 | GET | `/api/like/check/{postId}` | P1 ⭐ |

**响应数据**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": true
}
```

---

### 4.3 收藏/取消收藏
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 收藏/取消收藏 | POST | `/api/collect/{postId}` | P0 🔥 |

**实现要点**：
- 验证用户是否登录
- 检查是否已收藏
- 已收藏则取消收藏
- 未收藏则收藏

---

### 4.4 获取我的收藏列表
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取我的收藏 | GET | `/api/collect/my` | P1 ⭐ |

**响应数据**：
- 返回分页的收藏文章列表
- 包含文章完整信息

---

## 🏷️ 模块五：标签管理模块

### 5.1 获取所有标签
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取所有标签 | GET | `/api/tag/list` | P1 ⭐ |

---

### 5.2 获取某标签下的文章
| 功能 | 方法 | 路径 | 优先级 |
| :--- | :--- | :--- | :--- |
| 获取标签下文章 | GET | `/api/tag/{tagId}/posts` | P1 ⭐ |

---

## 🔒 模块六：Spring Security + JWT（后续版本）

### 6.1 启用 Spring Security
- 取消注释 pom.xml 中的 Spring Security 依赖
- 配置 SecurityConfig
- 配置密码加密器（BCrypt）

### 6.2 JWT 工具类
- 创建 JwtUtils.java
- 生成 Token
- 验证 Token
- 从 Token 中获取用户信息

### 6.3 JWT 拦截器/过滤器
- 创建 JwtAuthenticationFilter
- 验证请求头中的 Token
- 验证 Token 有效性
- 将用户信息放入 SecurityContext

### 6.4 接口权限控制
- 登录注册接口：无需认证
- 获取资源接口：无需认证
- 发布/修改/删除接口：需要认证
- 管理员接口：需要 admin 角色

---

## 📝 开发建议顺序

### 第一阶段：基础功能（无认证）
1. ✅ 用户注册
2. ✅ 用户登录（简单版，不做 Security）
3. ✅ 发布文章
4. ✅ 获取文章列表/详情
5. ✅ 发表评论
6. ✅ 点赞/收藏

### 第二阶段：完善功能
7. ✅ 更新/删除文章
8. ✅ 删除评论
9. ✅ 我的文章/我的收藏
10. ✅ 标签功能

### 第三阶段：安全加固
11. ✅ 启用 Spring Security
12. ✅ 集成 JWT
13. ✅ 接口权限控制

---

## 📌 注意事项

### 开发规范
1. 所有接口返回统一 Result<T> 格式
2. 使用 MyBatis Plus 提供的方法
3. 业务异常使用 BusinessException
4. 自动填充 createTime 和 updateTime
5. 使用逻辑删除，不要物理删除
6. 更新文档后再提交代码

### 数据库操作注意
- 添加数据前验证关联数据是否存在
- 删除数据时注意关联数据的处理
- 更新数据时注意更新时间自动填充

### 代码提交前检查
- [ ] 代码是否编译通过
- [ ] 是否测试接口是否正常
- [ ] 是否更新了相关文档
- [ ] 是否提交到 GitHub

---

## 📚 参考文档

- [README.md](./README.md) - 项目说明文档
- [campus_blog.md](./campus_blog.md) - 项目计划书
- [CLAUDE.md](./CLAUDE.md) - AI 协作文档

---

**文档版本**：v1.0
**创建日期**：2026-04-24

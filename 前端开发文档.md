# 校园博客论坛系统 - 前端开发文档

---

## 📋 文档信息

| 属性 | 内容 |
| :--- | :--- |
| **项目名称** | 校园博客论坛系统 - 前端 |
| **后端版本** | v1.35 |
| **最后更新** | 2026-04-27 |
| **API 文档地址** | http://localhost:8825/api/doc.html |

---

## 1. 项目概述

### 1.1 技术栈

| 技术 | 版本 | 说明 |
| :--- | :--- | :--- |
| **HTML5** | - | 页面结构 |
| **CSS3** | - | 页面样式 |
| **JavaScript (ES6+)** | ES Modules | 前端逻辑（模块化） |
| **Bootstrap** | 5.x | UI 框架 |
| **Axios** | 1.x | HTTP 请求库 |
| **Marked.js** | 9.x | Markdown 解析 |
| **Highlight.js** | 11.x | 代码高亮 |
| **DOMPurify** | 3.x | HTML 净化（XSS 防护） |

### 1.2 项目结构（推荐）

```
frontend/
├── index.html                 # 首页（文章列表）
├── login.html                 # 登录页
├── register.html              # 注册页
├── password-reset.html        # 密码重置页
├── post-detail.html           # 文章详情页
├── post-edit.html             # 发布/编辑文章页
├── post-search.html           # 高级搜索页
├── profile.html               # 个人中心
├── profile-edit.html          # 资料修改页
├── drafts.html                # 草稿箱
├── notifications.html         # 通知中心
├── circle.html                # 校友圈首页
├── circle-detail.html         # 校友圈动态详情
├── circle-post.html           # 发布校友圈动态
├── tags.html                  # 标签列表页
├── tag-posts.html             # 标签下的文章列表
├── search.html                # 搜索结果页
├── user-profile.html          # 其他用户主页
├── followers.html             # 粉丝列表
├── following.html             # 关注列表
├── collect.html               # 我的收藏
├── report.html                # 举报页面
├── messages.html              # 私信页面
├── topics.html                # 话题页面
├── admin/
│   ├── index.html             # 管理后台首页
│   ├── reports.html           # 举报管理
│   ├── users.html             # 用户管理
│   ├── posts.html             # 文章管理
│   └── statistics.html        # 统计数据
├── css/
│   └── style.css              # 自定义样式
├── js/
│   ├── api.js                 # API 调用封装（所有模块）
│   ├── state.js               # 全局状态管理（发布-订阅）
│   ├── auth.js                # 认证相关（Token 管理）
│   ├── request.js             # Axios 封装（含拦截器）
│   ├── marked.js              # Markdown 渲染配置
│   ├── toast.js               # Toast 提示组件
│   ├── validator.js           # 表单前端校验
│   └── utils.js               # 工具函数
└── assets/
    └── images/                # 图片资源
```

### 1.3 架构说明

> ⚠️ **重要提示**：本项目后端有 21 张表、20 个 Controller，建议前端采用**模块化开发**。如果继续使用传统多页面 + 全局 script 的方式，代码会迅速变得难以维护。

**推荐方案**：

| 方案 | 适用场景 | 优点 | 缺点 |
| :--- | :--- | :--- | :--- |
| **Vue 3 + Vite** | 竞赛/生产 | 高可维护性、组件化、生态完善 | 学习成本 |
| **原生 ES Modules** | 快速原型 | 无需构建工具、直接运行 | 模块化需自行设计 |

**如使用原生 JS 模块化**：
- 每个页面用独立 JS 文件
- 公共功能抽取到 `js/utils.js`、`js/state.js`
- 使用 ES6 `import/export`
- 生产环境可用 http-server 或 Vite 做简单构建

### 1.3.1 数据库变更（v1.35）

| 变更 | 说明 |
| :--- | :--- |
| **新增表** | `blog_draft_tag` - 草稿标签关联表 |
| **字段变更** | `blog_post.view_count` 由 INT 变更为 BIGINT |
| **索引调整** | 部分索引名称已规范化重命名，保持命名一致 |

---

## 1.4 环境配置

### 开发环境

```bash
# 启动前端（假设后端运行在 8825 端口）
cd frontend
npx http-server -p 3000 -c-1

# 或使用 live-server（热更新）
npx live-server --port=3000
```

**API 地址**：
- 开发环境后端：`http://localhost:8825/api`
- 前端页面：`http://localhost:3000`

### 生产环境

```javascript
// api.js 中根据环境切换
const API_BASE = import.meta.env.PROD
  ? 'https://your-domain.com/api'
  : 'http://localhost:8825/api';
```

---

## 2. 全局状态管理

### 2.1 简单的状态管理模式（发布-订阅）

```javascript
// js/state.js
const createStore = (initialState) => {
  let state = initialState;
  const listeners = new Set();

  return {
    getState: () => state,
    setState: (newState) => {
      state = { ...state, ...newState };
      listeners.forEach(listener => listener(state));
    },
    subscribe: (listener) => {
      listeners.add(listener);
      return () => listeners.delete(listener);
    }
  };
};

// 全局状态
window.appState = createStore({
  user: null,           // { id, username, nickname, avatar, ... }
  token: null,
  refreshToken: null,
  unreadCount: 0,       // 未读通知数
  isLoggedIn: false
});

// 状态变更通知
window.stateChanged = () => {
  // 更新导航栏等全局UI
  updateNavBar();
};
```

### 2.2 状态更新示例

```javascript
// 登录成功后
appState.setState({
  user: res.data,
  token: res.data.token,
  refreshToken: res.data.refreshToken,
  isLoggedIn: true
});
stateChanged();
```

---

## 2. API 基础配置

### 2.1 基础信息

| 配置项 | 值 |
| :--- | :--- |
| **API 基础地址** | `http://localhost:8825/api` |
| **Content-Type** | `application/json` |
| **字符编码** | `UTF-8` |
| **后端端口** | 8825 |

### 2.2 统一响应格式

所有接口返回统一格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... },
  "timestamp": 1714067200000
}
```

**状态码说明**：

| code | 说明 |
| :--- | :--- |
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录（需登录） |
| 403 | 无权限/账户锁定 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 2.3 Axios 封装示例（完善版）

```javascript
// js/request.js
import { showToast } from './toast.js';

const API_BASE = 'http://localhost:8825/api';

// 创建 Axios 实例
const request = axios.create({
  baseURL: API_BASE,
  timeout: 15000,
  headers: {
    'Content-Type': 'application/json'
  }
});

// 请求队列（防止并发刷 Token）
let isRefreshing = false;
let refreshSubscribers = [];

// 订阅 token 刷新
const subscribeTokenRefresh = (callback) => {
  refreshSubscribers.push(callback);
};

// 执行刷新后的回调
const onTokenRefreshed = (token) => {
  refreshSubscribers.forEach(cb => cb(token));
  refreshSubscribers = [];
};

// 刷新 Token
const refreshToken = async () => {
  const refreshToken = localStorage.getItem('refreshToken');
  if (!refreshToken) throw new Error('No refresh token');

  const res = await axios.post(`${API_BASE}/user/refresh`, null, {
    headers: { 'Authorization': `Bearer ${refreshToken}` }
  });
  return res.data.data;
};

// 请求拦截器：添加 Token
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token;
    }
    // 显示 loading（可选）
    return config;
  },
  error => Promise.reject(error)
);

// 响应拦截器：统一处理错误 + Token 自动刷新
request.interceptors.response.use(
  response => response.data,
  async error => {
    const originalRequest = error.config;

    // 401 且不是刷新 Token 请求
    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        // 等待刷新完成
        return new Promise(resolve => {
          subscribeTokenRefresh(token => {
            originalRequest.headers['Authorization'] = 'Bearer ' + token;
            resolve(request(originalRequest));
          });
        });
      }

      originalRequest._retry = true;
      isRefreshing = true;

      try {
        const newToken = await refreshToken();
        localStorage.setItem('token', newToken);
        onTokenRefreshed(newToken);
        isRefreshing = false;

        originalRequest.headers['Authorization'] = 'Bearer ' + newToken;
        return request(originalRequest);
      } catch (refreshError) {
        isRefreshing = false;
        // 刷新失败，清除登录状态
        localStorage.removeItem('token');
        localStorage.removeItem('refreshToken');
        window.location.href = '/login.html';
        return Promise.reject(refreshError);
      }
    }

    // 其他错误
    const message = error.response?.data?.message || '网络错误，请稍后重试';
    showToast(message, 'error');
    return Promise.reject(error.response?.data || error);
  }
);

export default request;
```

### 2.4 Toast 提示组件

```javascript
// js/toast.js
export function showToast(message, type = 'info', duration = 3000) {
  // 移除已存在的 toast
  const existing = document.querySelector('.toast-container');
  if (existing) existing.remove();

  const toast = document.createElement('div');
  toast.className = `toast-container position-fixed top-0 end-0 p-3`;
  toast.innerHTML = `
    <div class="toast show align-items-center text-white bg-${type === 'error' ? 'danger' : type} border-0" role="alert">
      <div class="d-flex">
        <div class="toast-body">${message}</div>
        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
      </div>
    </div>
  `;
  document.body.appendChild(toast);

  setTimeout(() => toast.remove(), duration);
}

export function showConfirm(message, onConfirm) {
  if (confirm(message)) {
    onConfirm();
  }
}
```

### 2.5 表单校验

```javascript
// js/validator.js
export const validators = {
  required: (value) => value?.trim() ? null : '此字段必填',
  minLength: (min) => (value) => value?.length >= min ? null : `至少${min}个字符`,
  maxLength: (max) => (value) => value?.length <= max ? null : `最多${max}个字符`,
  password: (value) => {
    if (!value || value.length < 8) return '至少8位';
    const types = [/[a-z]/, /[A-Z]/, /[0-9]/, /[^a-zA-Z0-9]/].filter(t => t.test(value));
    return types.length >= 3 ? null : '需包含大小写字母+数字/特殊字符中的3种';
  },
  email: (value) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value) ? null : '邮箱格式不正确',
  url: (value) => !value || /^https?:\/\/.+/.test(value) ? null : 'URL 格式不正确'
};

export function validateForm(data, rules) {
  const errors = {};
  for (const [field, rulesList] of Object.entries(rules)) {
    for (const rule of rulesList) {
      const error = rule(data[field]);
      if (error) {
        errors[field] = error;
        break;
      }
    }
  }
  return errors;
}
```

### 2.6 API 调用封装示例（完整版）

```javascript
// js/api.js - 完整的 API 调用封装
const API_BASE = 'http://localhost:8825/api';

const axiosInstance = axios.create({
  baseURL: API_BASE,
  timeout: 15000,
  headers: { 'Content-Type': 'application/json' }
});

// 请求拦截器：添加 Token
axiosInstance.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token;
    }
    return config;
  },
  error => Promise.reject(error)
);

// 响应拦截器：统一错误处理
axiosInstance.interceptors.response.use(
  response => response.data,
  error => {
    const res = error.response?.data;
    if (res?.code === 401) {
      localStorage.removeItem('token');
      localStorage.removeItem('refreshToken');
      window.location.href = '/login.html';
    }
    return Promise.reject(res || error);
  }
);

// ========== 用户模块 ==========
const userAPI = {
  register: (data) => axiosInstance.post('/user/register', data),
  login: (data) => axiosInstance.post('/user/login', data),
  refreshToken: () => axiosInstance.post('/user/refresh', null, {
    headers: { 'Authorization': `Bearer ${localStorage.getItem('refreshToken')}` }
  }),
  getUserById: (id) => axiosInstance.get('/user/' + id),
  changePassword: (data) => axiosInstance.put('/user/password', data),
  search: (params) => axiosInstance.get('/user/search', { params }),
  updateProfile: (data) => axiosInstance.put('/user/profile', data),
  updateAvatar: (data) => axiosInstance.put('/user/avatar', data),
  sendResetCode: (data) => axiosInstance.post('/user/send-code', data),
  resetPassword: (data) => axiosInstance.post('/user/reset-password', data)
};

// ========== 文章模块 ==========
const postAPI = {
  create: (data) => axiosInstance.post('/post', data),
  update: (id, data) => axiosInstance.put('/post/' + id, data),
  delete: (id) => axiosInstance.delete('/post/' + id),
  getDetail: (id) => axiosInstance.get('/post/' + id),
  getList: (params) => axiosInstance.get('/post/list', { params }),
  incrementView: (id) => axiosInstance.put('/post/' + id + '/view'),
  saveDraft: (data) => axiosInstance.post('/post/draft', data),
  getLatestDraft: () => axiosInstance.get('/post/draft/latest'),
  deleteDraft: (draftId) => axiosInstance.delete('/post/draft/' + draftId),
  getDraft: (draftId) => axiosInstance.get('/post/draft/' + draftId),
  advancedSearch: (params) => axiosInstance.get('/post/search/advanced', { params }),
  getSearchSuggestions: (params) => axiosInstance.get('/post/search/suggest', { params }),
  getMyPosts: (params) => axiosInstance.get('/post/my', { params })
};

// ========== 评论模块 ==========
const commentAPI = {
  create: (data) => axiosInstance.post('/comment', data),
  getByPostId: (postId) => axiosInstance.get('/comment/post/' + postId),
  delete: (commentId) => axiosInstance.delete('/comment/' + commentId),
  getMyComments: (params) => axiosInstance.get('/comment/my', { params })
};

// ========== 点赞模块 ==========
const likeAPI = {
  toggle: (postId) => axiosInstance.post('/like/' + postId),
  checkStatus: (postId) => axiosInstance.get('/like/check/' + postId),
  getMyLikes: (params) => axiosInstance.get('/like/my', { params })
  // 返回: { liked: boolean, likeCount: number }
};

// ========== 收藏模块 ==========
const collectAPI = {
  toggle: (postId) => axiosInstance.post('/collect/' + postId),
  checkStatus: (postId) => axiosInstance.get('/collect/check/' + postId),
  // 返回: { collected: boolean }
  getMyCollections: (params) => axiosInstance.get('/collect/my', { params })
};

// ========== 关注模块 ==========
const followAPI = {
  toggle: (data) => axiosInstance.post('/follow', data),  // data = { targetUserId: xxx }
  unfollow: (targetUserId) => axiosInstance.delete('/follow/' + targetUserId),
  checkStatus: (targetUserId) => axiosInstance.get('/follow/check/' + targetUserId),
  getFollowers: (userId) => axiosInstance.get('/follow/followers/' + userId),
  getFollowing: (userId) => axiosInstance.get('/follow/following/' + userId),
  getCounts: (userId) => axiosInstance.get('/follow/counts/' + userId)
};

// ========== 通知模块 ==========
const notificationAPI = {
  getList: (params) => axiosInstance.get('/notification/list', { params }),
  getUnreadCount: () => axiosInstance.get('/notification/unread-count'),
  markAsRead: (id) => axiosInstance.put('/notification/' + id + '/read'),
  markAllAsRead: () => axiosInstance.put('/notification/read-all'),
  delete: (id) => axiosInstance.delete('/notification/' + id)
};

// ========== 热门/趋势模块 ==========
const trendingAPI = {
  getHotPosts: (params) => axiosInstance.get('/trending/posts', { params }),
  getHotTags: () => axiosInstance.get('/trending/hot-tags')
};

// ========== 标签模块 ==========
const tagAPI = {
  listAll: () => axiosInstance.get('/tag/list'),
  create: (data) => axiosInstance.post('/tag', data),  // data = { name: "标签名" }
  delete: (tagId) => axiosInstance.delete('/tag/' + tagId)  // 仅管理员可用
};

// ========== 校友圈模块 ==========
const circleAPI = {
  createPost: (data) => axiosInstance.post('/circle/post', data),
  deletePost: (postId) => axiosInstance.delete('/circle/post/' + postId),
  getRecommendFeed: (page, pageSize) => axiosInstance.get('/circle/feed/recommend', {
    params: { page, pageSize }
  }),
  getFollowingFeed: (page, pageSize) => axiosInstance.get('/circle/feed/following', {
    params: { page, pageSize }
  }),
  getPostDetail: (postId) => axiosInstance.get('/circle/post/' + postId),
  toggleLike: (postId) => axiosInstance.post('/circle/like/' + postId),
  checkLikeStatus: (postId) => axiosInstance.get('/circle/like/check/' + postId),
  getComments: (postId) => axiosInstance.get('/circle/comment/' + postId),
  createComment: (data) => axiosInstance.post('/circle/comment', data),
  deleteComment: (commentId) => axiosInstance.delete('/circle/comment/' + commentId),
  repost: (postId, content) => axiosInstance.post('/circle/repost/' + postId, null, {
    params: { content }
  }),
  search: (keyword, page, pageSize) => axiosInstance.get('/circle/search', {
    params: { keyword, page, pageSize }
  })
};

// ========== 媒体模块 ==========
const mediaAPI = {
  upload: (formData) => axiosInstance.post('/media/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  }),
  uploadMultiple: (formData) => axiosInstance.post('/media/upload/multiple', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  }),
  delete: (mediaId) => axiosInstance.delete('/media/' + mediaId),
  getInfo: (mediaId) => axiosInstance.get('/media/' + mediaId),
  bindPost: (postId, mediaIds) => axiosInstance.post('/media/bind/' + postId, mediaIds),
  getPostMedia: (postId) => axiosInstance.get('/media/post/' + postId),
  getUserMediaList: (params) => axiosInstance.get('/media/list', { params })
};

// ========== 举报模块 ==========
const reportAPI = {
  create: (data) => axiosInstance.post('/report', data),
  // data: { targetType: "post"|"comment"|"circle"|"user", targetId: xxx, reason: "..." }
  getMyReports: (params) => axiosInstance.get('/report/my', { params })
};

// ========== 私信模块 ==========
const messageAPI = {
  send: (data) => axiosInstance.post('/message/send', data),
  // data: { receiverId: xxx, content: "..." }
  getReceived: (params) => axiosInstance.get('/message/received', { params }),
  getSent: (params) => axiosInstance.get('/message/sent', { params }),
  markAsRead: (id) => axiosInstance.put('/message/' + id + '/read'),
  delete: (id) => axiosInstance.delete('/message/' + id),
  getUnreadCount: () => axiosInstance.get('/message/unread-count')
};

// ========== 话题模块 ==========
const topicAPI = {
  create: (data) => axiosInstance.post('/topic', data),
  // data: { name: "话题名", description: "描述" }
  getList: (params) => axiosInstance.get('/topic/list', { params }),
  getHot: () => axiosInstance.get('/topic/hot'),
  getDetail: (topicId) => axiosInstance.get('/topic/' + topicId),
  getPosts: (topicId, params) => axiosInstance.get('/topic/' + topicId + '/posts', { params })
};

// ========== 管理后台 ==========
const adminAPI = {
  getPendingReports: (params) => axiosInstance.get('/admin/reports/pending', { params }),
  getReportDetail: (reportId) => axiosInstance.get('/admin/reports/' + reportId),
  handleReport: (reportId, data) => axiosInstance.put('/admin/reports/' + reportId, data),
  // data: { status: 1|2, handlerResult: "..." }
};

// 导出
window.api = {
  user: userAPI,
  post: postAPI,
  comment: commentAPI,
  like: likeAPI,
  collect: collectAPI,
  follow: followAPI,
  notification: notificationAPI,
  trending: trendingAPI,
  tag: tagAPI,
  circle: circleAPI,
  media: mediaAPI,
  report: reportAPI,
  admin: adminAPI,
  message: messageAPI,
  topic: topicAPI
};

export { api as default };
```

---

## 3. 接口详细文档（完整版）

> ⚠️ **提示**：以下接口覆盖后端全部 20 个 Controller。前端应实现所有接口以保证功能完整性。

---

### 3.1 用户模块

#### 3.1.1 用户注册

```
POST /api/user/register
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| username | string | ✅ | 用户名（3-20字符） |
| password | string | ✅ | 密码（8位+大小写字母+数字/特殊字符中的3种） |
| nickname | string | ❌ | 昵称（最长30字符） |
| email | string | ❌ | 邮箱 |

**请求示例**：
```json
{
  "username": "zhangsan",
  "password": "Abc123!@#",
  "nickname": "张三",
  "email": "zhangsan@example.com"
}
```

**响应**：`{ "code": 200, "data": { "id": 1, "username": "zhangsan" } }`

---

#### 3.1.2 用户登录

```
POST /api/user/login
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| username | string | ✅ | 用户名 |
| password | string | ✅ | 密码 |

**响应**：
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "admin",
    "nickname": "管理员",
    "avatar": "https://example.com/avatar.jpg",
    "role": "admin",
    "token": "eyJ...",
    "refreshToken": "eyJ..."
  }
}
```

**错误**：账户锁定返回 `code: 403` + `"账户已锁定，请在15分钟后重试"`

---

#### 3.1.3 刷新 Token

```
POST /api/user/refresh
```
**请求头**：`Authorization: Bearer <refreshToken>`

**响应**：`{ "code": 200, "data": { "token": "新token", "refreshToken": "新refreshToken" } }`

---

#### 3.1.4 获取用户信息

```
GET /api/user/{id}
```

**需要登录**：是

**响应**：
```json
{
  "data": {
    "id": 1,
    "username": "admin",
    "nickname": "管理员",
    "avatar": "https://example.com/avatar.jpg",
    "email": "admin@example.com",
    "role": "admin",
    "status": 1,
    "createTime": "2026-04-25T10:00:00",
    "updateTime": "2026-04-26T10:00:00",
    "followerCount": 100,
    "followingCount": 50,
    "isFollowing": false
  }
}
```

---

#### 3.1.5 修改密码

```
PUT /api/user/password
```

**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| oldPassword | string | ✅ | 旧密码 |
| newPassword | string | ✅ | 新密码（复杂度同注册） |

**响应**：`{ "code": 200, "message": "操作成功" }`

---

#### 3.1.6 用户搜索

```
GET /api/user/search
```

**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | ✅ | 搜索关键词（匹配用户名/昵称） |
| page | int | ❌ | 页码（默认1） |
| pageSize | int | ❌ | 每页数量（默认10） |

**响应**：分页返回用户列表 `{ "data": { "records": [...], "total": 100, "current": 1 } }`

---

#### 3.1.7 更新个人资料

```
PUT /api/user/profile
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| nickname | string | ❌ | 昵称（最长30字符） |
| bio | string | ❌ | 个人简介（最长200字符） |

**响应**：`{ "code": 200, "message": "操作成功" }`

---

#### 3.1.8 更新头像

```
PUT /api/user/avatar
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| avatar | string | ✅ | 头像URL |

**响应**：`{ "code": 200, "message": "操作成功" }`

---

#### 3.1.9 发送密码重置验证码

```
POST /api/user/send-code
```
**需要登录**：否

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| email | string | ✅ | 注册邮箱 |

**响应**：`{ "code": 200, "message": "验证码已发送" }`

---

#### 3.1.10 重置密码

```
POST /api/user/reset-password
```
**需要登录**：否

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| email | string | ✅ | 注册邮箱 |
| code | string | ✅ | 验证码 |
| newPassword | string | ✅ | 新密码（复杂度同注册） |

**响应**：`{ "code": 200, "message": "密码重置成功" }`

---

### 3.2 文章模块

#### 3.2.1 发布文章

```
POST /api/post
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| title | string | ✅ | 标题（最长200字符） |
| content | string | ✅ | 内容（Markdown，最长50000字符） |
| summary | string | ❌ | 摘要（最长500字符） |
| category | string | ❌ | 分类（默认"默认分类"） |
| tagIds | array | ❌ | 标签ID列表 |

**响应**：`{ "code": 200, "data": 文章ID }`

---

#### 3.2.2 更新文章

```
PUT /api/post/{id}
```
**需要登录**：是（作者或管理员）

---

#### 3.2.3 删除文章

```
DELETE /api/post/{id}
```
**需要登录**：是（作者或管理员）

---

#### 3.2.4 获取文章详情

```
GET /api/post/{id}
```
**需要登录**：否

---

#### 3.2.5 获取文章列表

```
GET /api/post/list
```
**需要登录**：否

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| page | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10，最大100） |
| keyword | string | 搜索关键词 |
| category | string | 分类筛选 |
| userId | long | 作者ID筛选 |
| tagId | long | 标签ID筛选 |

---

#### 3.2.6 增加阅读量

```
PUT /api/post/{id}/view
```
防刷机制：已登录用户每篇1分钟1次，未登录IP+UA每篇1分钟1次

---

#### 3.2.7 保存草稿

```
POST /api/post/draft
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| title | string | ❌ | 草稿标题 |
| content | string | ❌ | 草稿内容 |
| summary | string | ❌ | 摘要 |
| category | string | ❌ | 分类 |
| tagIds | array | ❌ | 标签ID |

**响应**：`{ "code": 200, "data": 草稿ID }`

---

#### 3.2.8 获取我的最新草稿

```
GET /api/post/draft/latest
```
**需要登录**：是

---

#### 3.2.9 删除草稿

```
DELETE /api/post/draft/{draftId}
```
**需要登录**：是

---

#### 3.2.10 获取指定草稿

```
GET /api/post/draft/{draftId}
```
**需要登录**：是

---

#### 3.2.11 文章高级搜索

```
GET /api/post/search/advanced
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| keyword | string | 关键词 |
| category | string | 分类 |
| tagId | long | 标签ID |
| userId | long | 作者ID |
| sortBy | string | 排序字段：`time`/`view`/`like`（默认 `time`） |
| page | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10，最大100） |

> ⚠️ 注意：当前不支持日期范围筛选。

---

#### 3.2.12 搜索建议（标题自动补全）

```
GET /api/post/search/suggest
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| keyword | string | 关键词 |

**响应**：`{ "data": ["Spring Boot 入门", "Spring Boot 进阶"] }`

---

#### 3.2.13 获取我的文章列表

```
GET /api/post/my
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

### 3.3 评论模块

#### 3.3.1 发表评论

```
POST /api/comment
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| postId | long | ✅ | 文章ID |
| content | string | ✅ | 评论内容（最长1000字符） |
| parentId | long | ❌ | 父评论ID（回复时） |

---

#### 3.3.2 获取文章评论列表

```
GET /api/comment/post/{postId}
```
返回树形结构（一级 + 二级 replies）

---

#### 3.3.3 删除评论

```
DELETE /api/comment/{commentId}
```
级联删除所有子评论

---

#### 3.3.4 获取我的评论列表

```
GET /api/comment/my
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

### 3.4 点赞模块

#### 3.4.1 点赞/取消点赞

```
POST /api/like/{postId}
```
**需要登录**：是

**响应**：`{ "data": { "action": "like"|"unlike", "likeCount": 11 } }`

---

#### 3.4.2 检查点赞状态

```
GET /api/like/check/{postId}
```
**响应**：`{ "data": { "liked": true, "likeCount": 10 } }`

---

#### 3.4.3 获取我的点赞列表

```
GET /api/like/my
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

### 3.5 收藏模块

#### 3.5.1 收藏/取消收藏

```
POST /api/collect/{postId}
```
**需要登录**：是

**响应**：`{ "data": { "action": "collect"|"uncollect" } }`

---

#### 3.5.2 检查收藏状态

```
GET /api/collect/check/{postId}
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| postId | path | 文章ID |

**响应**：`{ "data": { "collected": true/false } }`

---

#### 3.5.3 获取我的收藏列表

```
GET /api/collect/my
```
**需要登录**：是

---

### 3.6 关注模块

#### 3.6.1 关注用户

```
POST /api/follow
```
**需要登录**：是

**请求**：`{ "targetUserId": 2 }`

**响应**：
```json
{
  "data": {
    "following": true,
    "followingCount": 10,
    "followerCount": 100
  }
}
```

---

#### 3.6.2 取消关注

```
DELETE /api/follow/{targetUserId}
```
**需要登录**：是

---

#### 3.6.3 检查关注状态

```
GET /api/follow/check/{targetUserId}
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| targetUserId | path | 目标用户ID |

**响应**：
```json
{
  "data": {
    "following": true,
    "action": "already_following",
    "followerCount": 100,
    "followingCount": 10
  }
}
```

| action 值 | 说明 |
| :--- | :--- |
| `follow` | 可关注 |
| `already_following` | 已关注 |
| `unfollow` | 可取消关注 |
| `not_following` | 未关注（当前用户未登录或未关注对方） |

---

#### 3.6.4 获取粉丝列表

```
GET /api/follow/followers/{userId}
```
**需要登录**：是

**响应**：`{ "data": [{ "id": 1, "username": "...", "nickname": "...", "avatar": "..." }] }`

---

#### 3.6.5 获取关注列表

```
GET /api/follow/following/{userId}
```
**需要登录**：是

---

#### 3.6.6 获取粉丝/关注数量

```
GET /api/follow/counts/{userId}
```
**响应**：`{ "data": { "followingCount": 10, "followerCount": 100 } }`

---

### 3.7 通知模块

#### 3.7.1 获取通知列表

```
GET /api/notification/list
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10，最大100） |

**响应**：
```json
{
  "data": {
    "records": [{
      "id": 1,
      "type": "like",
      "title": "有人点赞了你的文章",
      "content": "用户张三点赞了你的文章《Spring Boot 入门》",
      "fromUser": { "id": 2, "username": "zhangsan", "nickname": "张三", "avatar": "..." },
      "targetType": "post",
      "targetId": 10,
      "isRead": 0,
      "createTime": "2026-04-26T10:00:00",
      "timeAgo": "5分钟前"
    }],
    "total": 20,
    "size": 10,
    "current": 1,
    "pages": 2
  }
}
```

| isRead | 说明 |
| :--- | :--- |
| 0 | 未读 |
| 1 | 已读 |

---

#### 3.7.2 获取未读通知数量

```
GET /api/notification/unread-count
```
**需要登录**：是

**响应**：`{ "data": 5 }`

> **前端注意**：此接口用于更新顶部导航栏的未读数红点，建议在页面加载和切换时调用

---

#### 3.7.3 标记单条通知为已读

```
PUT /api/notification/{id}/read
```
**需要登录**：是

---

#### 3.7.4 标记所有通知为已读

```
PUT /api/notification/read-all
```
**需要登录**：是

---

#### 3.7.5 删除通知

```
DELETE /api/notification/{id}
```
**需要登录**：是

---

### 3.8 热门/趋势模块

#### 3.8.1 获取热门文章

```
GET /api/trending/posts
```
**需要登录**：否

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

**响应**：
```json
{
  "data": {
    "records": [{
      "id": 1,
      "title": "标题",
      "summary": "摘要",
      "category": "分类",
      "viewCount": 100,
      "likeCount": 10,
      "commentCount": 5,
      "score": 250.0,
      "createTime": "2026-04-26T10:00:00"
    }],
    "total": 50,
    "size": 10,
    "current": 1
  }
}
```

---

#### 3.8.2 获取热门标签

```
GET /api/trending/hot-tags
```
**需要登录**：否

**响应**：
```json
{
  "data": {
    "records": [{ "id": 1, "name": "Java", "postCount": 50 }],
    "total": 20,
    "size": 20,
    "current": 1
  }
}
```

---

#### 3.8.3 更新热门数据

```
POST /api/trending/update/{postId}
```
**需要登录**：是（管理员）

---

### 3.9 标签模块

#### 3.9.1 获取所有标签

```
GET /api/tag/list
```

**响应**：`{ "data": [{ "id": 1, "name": "Java" }, { "id": 2, "name": "Spring Boot" }] }`

---

#### 3.9.2 创建标签

```
POST /api/tag
```
**需要登录**：是

**请求**：`{ "name": "新标签" }`

**响应**：`{ "data": { "id": 3, "name": "新标签" } }`

#### 3.9.3 删除标签（仅管理员）

```
DELETE /api/tag/{tagId}
```
**需要登录**：是（管理员）

**响应**：`{ "code": 200, "message": "操作成功" }`

---

### 3.10 校友圈模块

> ⚠️ **权限设计说明**：校友圈参考微博/推特的隐私控制模式，支持：
> - **可见性**：公开（所有人可见）/ 仅关注者（需关注才能看）/ 仅自己（仅自己能看）
> - **评论权限**：可以关闭评论
> - **转发权限**：可以禁止转发

#### 3.10.1 发布动态

```
POST /api/circle/post
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| content | string | ✅ | 动态内容（最长2000字符） |
| images | array | ❌ | 图片URL列表（最多9张） |
| location | string | ❌ | 位置 |
| tags | array | ❌ | 话题标签（如"#Java"） |
| repostId | long | ❌ | 转发原动态ID |
| visibility | int | ❌ | 可见性：0=公开(默认)，1=仅关注者，2=仅自己 |
| allowComment | int | ❌ | 是否允许评论：1=允许(默认)，0=不允许 |
| allowRepost | int | ❌ | 是否允许转发：1=允许(默认)，0=不允许 |

**visibility 可见性说明**：
| 值 | 说明 | 谁能看 |
| :--- | :--- | :--- |
| 0 | 公开 | 所有人 |
| 1 | 仅关注者 | 关注了作者的人 |
| 2 | 仅自己 | 只有作者自己能看 |

---

#### 3.10.2 删除动态

```
DELETE /api/circle/post/{postId}
```
**需要登录**：是

---

#### 3.10.3 获取推荐流

```
GET /api/circle/feed/recommend
```

**说明**：展示公开动态 + 自己发布的动态（包括仅自己可见的）

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| page | int | 页码（默认1） |
| pageSize | int | 每页数量（默认20） |

---

#### 3.10.4 获取关注流

```
GET /api/circle/feed/following
```
**需要登录**：是

**说明**：展示关注的人的动态，考虑可见性设置（仅关注者可见的动态需要关注才能看）

---

#### 3.10.5 获取动态详情

```
GET /api/circle/post/{postId}
```

**权限说明**：
- 公开动态：所有人可看
- 仅关注者：需要关注作者
- 仅自己：只有作者自己能看

**响应**：
```json
{
  "data": {
    "id": 1,
    "content": "动态内容",
    "contentType": 1,
    "location": "北京市",
    "visibility": 0,
    "allowComment": 1,
    "allowRepost": 1,
    "likeCount": 10,
    "commentCount": 5,
    "repostCount": 2,
    "viewCount": 100,
    "isLiked": false,
    "isReposted": false,
    "isTop": false,
    "user": { "id": 1, "nickname": "用户甲", "avatar": "..." },
    "imageUrls": ["https://..."],
    "tags": ["#话题1", "#话题2"],
    "createTime": "2026-04-26T10:00:00",
    "timeAgo": "5分钟前",
    "repostPost": {
      "id": 99,
      "content": "原动态内容",
      "visibility": 1,
      "user": { ... },
      "imageUrls": [...],
      "originalPostHidden": false
    },
    "originalPostHidden": false
  }
}
```

**响应字段说明**：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| contentType | int | 内容类型：1=纯文本，2=图片，3=转发 |
| location | string | 位置信息（可选） |
| visibility | int | 可见性：0=公开，1=仅关注者，2=仅自己 |
| allowComment | int | 是否允许评论：1=允许，0=不允许 |
| allowRepost | int | 是否允许转发：1=允许，0=不允许 |
| viewCount | int | 阅读量 |
| isTop | boolean | 是否置顶 |
| timeAgo | string | 相对时间（如"5分钟前"） |
| repostPost | object | 转发的原动态（如果有），包含完整原动态信息 |
| originalPostHidden | boolean | 转发的原动态是否无权查看（true=原动态已不可见） |

---

#### 3.10.6 校友圈点赞/取消点赞

```
POST /api/circle/like/{postId}
```
**需要登录**：是

**响应**：`{ "code": 200, "message": "操作成功" }`

---

#### 3.10.7 检查校友圈动态点赞状态

```
GET /api/circle/like/check/{postId}
```

**响应**：`{ "data": true }` 或 `{ "data": false }`

---

#### 3.10.8 获取动态评论列表

```
GET /api/circle/comment/{postId}
```

---

#### 3.10.9 发表评论

```
POST /api/circle/comment
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| postId | long | ✅ | 动态ID |
| content | string | ✅ | 评论内容（最长1000字符） |
| parentId | long | ❌ | 父评论ID |
| replyToUserId | long | ❌ | 回复目标用户ID |

---

#### 3.10.10 删除评论

```
DELETE /api/circle/comment/{commentId}
```
**需要登录**：是

---

#### 3.10.11 转发动态

```
POST /api/circle/repost/{postId}
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| content | string | ❌ | 转发的评论（最长500字符） |

---

#### 3.10.12 搜索动态

```
GET /api/circle/search
```
**需要登录**：否

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| keyword | string | ✅ | 搜索关键词 |
| page | int | 页码（默认1） |
| pageSize | int | 每页数量（默认20） |

---

### 3.11 媒体模块

#### 3.11.1 上传媒体文件

```
POST /api/media/upload
```
**需要登录**：是
**Content-Type**：`multipart/form-data`

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| file | file | 图片或视频文件 |

**响应**：
```json
{
  "data": {
    "id": 1,
    "url": "https://cdn.example.com/uploads/xxx.jpg",
    "type": "image",
    "size": 102400
  }
}
```

---

#### 3.11.2 批量上传媒体文件

```
POST /api/media/upload/multiple
```
**需要登录**：是
**最多上传9个文件**

---

#### 3.11.3 删除媒体文件

```
DELETE /api/media/{mediaId}
```
**需要登录**：是

---

#### 3.11.4 获取媒体信息

```
GET /api/media/{mediaId}
```
**需要登录**：是

**响应**：
```json
{
  "data": {
    "id": 1,
    "fileUrl": "https://cdn.example.com/media/2026/04/image.jpg",
    "thumbUrl": "https://cdn.example.com/media/2026/04/image_thumb.jpg",
    "fileSize": 102400,
    "width": 1920,
    "height": 1080
  }
}
```

#### 3.11.5 绑定文章媒体

```
POST /api/media/bind/{postId}
```
**需要登录**：是

**请求**：`{ "mediaIds": [1, 2, 3] }`（最多20个）

---

#### 3.11.6 获取文章的媒体列表

```
GET /api/media/post/{postId}
```
**需要登录**：是

---

#### 3.11.7 分页查询用户媒体列表

```
GET /api/media/list
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| page | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10，最大100） |
| fileType | string | 文件类型筛选：`image`/`video`/`audio`（可选） |

---

### 3.12 举报模块

#### 3.12.1 提交举报

```
POST /api/report
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| type | string | ✅ | 举报类型（post/comment/circle/user） |
| targetId | long | ✅ | 被举报目标ID |
| reason | string | ✅ | 举报原因（最长500字符） |

---

#### 3.12.2 获取我的举报列表

```
GET /api/report/my
```
**需要登录**：是

---

### 3.13 管理后台模块

#### 3.13.1 获取待处理举报列表

```
GET /api/admin/reports/pending
```
**需要登录**：是（管理员）

---

#### 3.13.2 获取举报详情

```
GET /api/admin/reports/{reportId}
```
**需要登录**：是（管理员）

---

#### 3.13.3 处理举报

```
PUT /api/admin/reports/{reportId}
```
**需要登录**：是（管理员）

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| status | string | ✅ | 处理状态（dismissed/handled） |
| handleComment | string | ❌ | 处理备注 |

---

### 3.14 私信模块

#### 3.14.1 发送私信

```
POST /api/message/send
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| receiverId | long | ✅ | 接收者ID |
| content | string | ✅ | 私信内容（最长1000字符） |

**响应**：`{ "code": 200, "message": "发送成功" }`

---

#### 3.14.2 获取收件箱

```
GET /api/message/received
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

#### 3.14.3 获取发件箱

```
GET /api/message/sent
```
**需要登录**：是

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

#### 3.14.4 标记私信为已读

```
PUT /api/message/{id}/read
```
**需要登录**：是

---

#### 3.14.5 删除私信

```
DELETE /api/message/{id}
```
**需要登录**：是

---

#### 3.14.6 获取未读私信数量

```
GET /api/message/unread-count
```
**需要登录**：是

**响应**：`{ "data": 5 }`

---

### 3.15 话题模块

#### 3.15.1 创建话题

```
POST /api/topic
```
**需要登录**：是

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| name | string | ✅ | 话题名称 |
| description | string | ❌ | 话题描述 |

**响应**：`{ "code": 200, "data": { "id": 1, "name": "话题名" } }`

---

#### 3.15.2 获取话题列表

```
GET /api/topic/list
```
**需要登录**：否

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认20） |

---

#### 3.15.3 获取热门话题

```
GET /api/topic/hot
```
**需要登录**：否

**响应**：`{ "data": [{ "id": 1, "name": "Java", "postCount": 100 }] }`

---

#### 3.15.4 获取话题详情

```
GET /api/topic/{topicId}
```
**需要登录**：否

---

#### 3.15.5 获取话题下的文章

```
GET /api/topic/{topicId}/posts
```
**需要登录**：否

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |

---

### 3.16 管理员模块（扩展）

#### 3.16.1 获取用户列表（管理员）

```
GET /api/admin/user/list
```
**需要登录**：是（管理员）

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |
| keyword | string | 搜索关键词（可选） |

---

#### 3.16.2 更新用户状态（管理员）

```
PUT /api/admin/user/{id}/status
```
**需要登录**：是（管理员）

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| status | int | ✅ | 状态（1=正常，0=禁用） |

---

#### 3.16.3 重置用户密码（管理员）

```
PUT /api/admin/user/{id}/reset-password
```
**需要登录**：是（管理员）

**响应**：`{ "code": 200, "message": "密码已重置为默认密码" }`

---

#### 3.16.4 封禁用户（管理员）

```
PUT /api/admin/user/{id}/ban
```
**需要登录**：是（管理员）

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| banReason | string | ❌ | 封禁原因 |
| banDays | int | ❌ | 封禁天数（默认永久） |

---

#### 3.16.5 获取文章列表（管理员）

```
GET /api/admin/post/list
```
**需要登录**：是（管理员）

| 参数 | 类型 | 说明 |
| :--- | :--- | :--- |
| pageNum | int | 页码（默认1） |
| pageSize | int | 每页数量（默认10） |
| keyword | string | 搜索关键词（可选） |

---

#### 3.16.6 删除文章（管理员）

```
DELETE /api/admin/post/{id}
```
**需要登录**：是（管理员）

---

#### 3.16.7 获取待审核文章列表

```
GET /api/admin/post/review-list
```
**需要登录**：是（管理员）

---

#### 3.16.8 审核通过文章

```
PUT /api/admin/post/{id}/approve
```
**需要登录**：是（管理员）

---

#### 3.16.9 审核拒绝文章

```
PUT /api/admin/post/{id}/reject
```
**需要登录**：是（管理员）

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| rejectReason | string | ✅ | 拒绝原因 |

---

#### 3.16.10 删除评论（管理员）

```
DELETE /api/admin/comment/{id}
```
**需要登录**：是（管理员）

---

#### 3.16.11 获取统计数据

```
GET /api/admin/statistics/platform
```
**需要登录**：是（管理员）

**响应**：
```json
{
  "data": {
    "userCount": 1000,
    "postCount": 5000,
    "commentCount": 20000,
    "activeUsers": 500
  }
}
```

---

## 4. 页面与模块清单

### 4.1 必需页面

| 页面 | 文件 | 说明 |
| :--- | :--- | :--- |
| 首页 | `index.html` | 文章列表、搜索、热门标签 |
| 登录 | `login.html` | 登录表单 |
| 注册 | `register.html` | 注册表单 |
| 文章详情 | `post-detail.html` | 文章内容、评论、点赞收藏 |
| 发布文章 | `post-edit.html` | Markdown 编辑器 |
| 个人中心 | `profile.html` | 我的文章、我的收藏、我的评论 |
| 资料修改 | `profile-edit.html` | 修改昵称、头像 |
| 通知中心 | `notifications.html` | 通知列表、未读数 |
| 校友圈 | `circle.html` | 推荐流、关注流 |
| 发布动态 | `circle-post.html` | 发布校友圈动态 |
| 草稿箱 | `drafts.html` | 草稿列表 |
| 标签页 | `tags.html` | 所有标签 |
| 私信 | `messages.html` | 私信收件箱/发件箱 |
| 话题 | `topics.html` | 话题列表/热门话题 |

### 4.2 进阶页面

| 页面 | 文件 | 说明 |
| :--- | :--- | :--- |
| 高级搜索 | `post-search.html` | 多条件筛选 |
| 其他用户主页 | `user-profile.html` | 关注/粉丝、文章列表 |
| 粉丝列表 | `followers.html` | 粉丝列表 |
| 关注列表 | `following.html` | 关注列表 |
| 我的收藏 | `collect.html` | 收藏文章列表 |
| 标签文章 | `tag-posts.html` | 某标签下的文章 |
| 举报页面 | `report.html` | 提交举报 |
| 管理后台 | `admin/index.html` | 管理员面板 |
| 举报管理 | `admin/reports.html` | 处理举报 |
| 用户管理 | `admin/users.html` | 用户管理（管理员） |
| 文章管理 | `admin/posts.html` | 文章管理（管理员） |
| 统计数据 | `admin/statistics.html` | 平台统计（管理员） |

---

## 5. 关键功能实现示例

### 5.1 导航栏（带未读通知数）

```html
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="index.html">校园博客</a>
    <div class="navbar-nav ms-auto">
      <a class="nav-link" href="notifications.html">
        通知
        <span id="unreadBadge" class="badge bg-danger d-none">0</span>
      </a>
      <a class="nav-link" href="profile.html" id="profileLink">个人中心</a>
      <a class="nav-link" href="#" id="logoutLink">退出</a>
    </div>
  </div>
</nav>
```

```javascript
// 更新未读通知数
async function updateUnreadCount() {
  const token = localStorage.getItem('token');
  if (!token) return;

  try {
    const res = await api.notification.getUnreadCount();
    const badge = document.getElementById('unreadBadge');
    if (res.data > 0) {
      badge.textContent = res.data;
      badge.classList.remove('d-none');
    } else {
      badge.classList.add('d-none');
    }
    // 更新全局状态
    appState.setState({ unreadCount: res.data });
    stateChanged();
  } catch (err) {
    // 忽略错误
  }
}

// 初始化
updateUnreadCount();
// 每60秒刷新一次
setInterval(updateUnreadCount, 60000);
```

### 5.2 Markdown 编辑器页面

```html
<!-- post-edit.html -->
<div class="container mt-4">
  <div class="row">
    <div class="col-md-8">
      <input type="text" class="form-control mb-3" id="postTitle" placeholder="文章标题">
      <textarea id="markdownEditor" class="form-control mb-3" rows="15" placeholder="文章内容（支持 Markdown）"></textarea>
    </div>
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h5>设置</h5>
          <div class="mb-3">
            <label class="form-label">分类</label>
            <input type="text" class="form-control" id="category" value="默认分类">
          </div>
          <div class="mb-3">
            <label class="form-label">标签</label>
            <div id="tagList" class="d-flex flex-wrap gap-2"></div>
          </div>
          <div class="mb-3">
            <label class="form-label">摘要</label>
            <textarea id="summary" class="form-control" rows="2"></textarea>
          </div>
          <button class="btn btn-primary me-2" onclick="publishPost()">发布</button>
          <button class="btn btn-outline-secondary" onclick="saveDraft()">保存草稿</button>
        </div>
      </div>
      <!-- 实时预览 -->
      <div class="card mt-3">
        <div class="card-header">预览</div>
        <div class="card-body" id="previewContent"></div>
      </div>
    </div>
  </div>
</div>

<script type="module">
  import { api } from './js/api.js';
  import { showToast } from './js/toast.js';

  // 实时预览
  document.getElementById('markdownEditor').addEventListener('input', (e) => {
    document.getElementById('previewContent').innerHTML = marked.parse(e.target.value);
  });

  // 发布文章
  window.publishPost = async function() {
    const title = document.getElementById('postTitle').value.trim();
    const content = document.getElementById('markdownEditor').value;
    const summary = document.getElementById('summary').value;
    const category = document.getElementById('category').value;

    if (!title) {
      showToast('请输入文章标题', 'error');
      return;
    }
    if (!content) {
      showToast('请输入文章内容', 'error');
      return;
    }

    try {
      const postId = await api.post.create({ title, content, summary, category });
      showToast('发布成功！', 'success');
      window.location.href = `post-detail.html?id=${postId.data}`;
    } catch (err) {
      showToast(err.message || '发布失败', 'error');
    }
  };

  // 保存草稿
  window.saveDraft = async function() {
    try {
      await api.post.saveDraft({
        title: document.getElementById('postTitle').value,
        content: document.getElementById('markdownEditor').value,
        summary: document.getElementById('summary').value,
        category: document.getElementById('category').value
      });
      showToast('草稿已保存', 'success');
    } catch (err) {
      showToast('保存失败', 'error');
    }
  };

  // 加载标签列表
  async function loadTags() {
    const res = await api.tag.listAll();
    // 渲染标签复选框
  }

  // 页面加载时检查草稿
  async function checkDraft() {
    const res = await api.post.getLatestDraft();
    if (res.data && res.data.title) {
      // 询问是否恢复草稿
      if (confirm('发现未保存的草稿，是否恢复？')) {
        document.getElementById('postTitle').value = res.data.title;
        document.getElementById('markdownEditor').value = res.data.content;
        document.getElementById('summary').value = res.data.summary || '';
      }
    }
  }

  loadTags();
  checkDraft();
</script>
```

### 5.3 校友圈页面

```html
<!-- circle.html -->
<div class="container mt-4">
  <div class="row">
    <!-- 侧边栏 -->
    <div class="col-md-3">
      <div class="card">
        <div class="card-body">
          <a href="circle-post.html" class="btn btn-primary w-100 mb-3">发布动态</a>
          <ul class="nav flex-column">
            <li class="nav-item"><a class="nav-link active" href="#" onclick="switchFeed('recommend')">推荐</a></li>
            <li class="nav-item"><a class="nav-link" href="#" onclick="switchFeed('following')">关注</a></li>
          </ul>
        </div>
      </div>
    </div>
    <!-- 动态列表 -->
    <div class="col-md-6">
      <div id="circleList"></div>
      <div class="text-center mt-3">
        <button class="btn btn-outline-primary" id="loadMoreBtn" onclick="loadMore()">加载更多</button>
      </div>
    </div>
  </div>
</div>

<script type="module">
  import { api } from './js/api.js';

  let currentFeed = 'recommend';
  let currentPage = 1;

  window.switchFeed = function(feed) {
    currentFeed = feed;
    currentPage = 1;
    loadCirclePosts();
  };

  async function loadCirclePosts() {
    const apiCall = currentFeed === 'recommend'
      ? api.circle.getRecommendFeed(currentPage)
      : api.circle.getFollowingFeed(currentPage);

    const res = await apiCall;
    renderCirclePosts(res.data);
  }

  function renderCirclePosts(posts) {
    const container = document.getElementById('circleList');
    container.innerHTML = posts.map(post => `
      <div class="card mb-3">
        <div class="card-body">
          <div class="d-flex">
            <img src="${post.user?.avatar || '/assets/images/default-avatar.png'}" class="rounded-circle me-2" width="40" height="40">
            <div class="flex-grow-1">
              <strong>${post.user?.nickname || '匿名用户'}</strong>
              <small class="text-muted">${post.timeAgo || post.createTime}</small>
            </div>
          </div>
          <p class="mt-2">${post.content}</p>
          ${post.imageUrls?.length > 0 ? `<img src="${post.imageUrls[0]}" class="img-fluid rounded mb-2">` : ''}
          <div class="d-flex gap-3">
            <button class="btn btn-sm btn-outline-primary" onclick="toggleLike(${post.id})">
              👍 ${post.likeCount || 0}
            </button>
            <button class="btn btn-sm btn-outline-secondary" onclick="window.location.href='circle-detail.html?id=${post.id}'">
              💬 ${post.commentCount || 0}
            </button>
            <button class="btn btn-sm btn-outline-info" onclick="repost(${post.id})">转发</button>
          </div>
        </div>
      </div>
    `).join('');
  }

  window.loadMore = function() {
    currentPage++;
    loadCirclePosts();
  };
</script>
```

---

## 6. 认证与权限

### 6.1 Token 存储

| Key | 说明 | 有效期 |
| :--- | :--- | :--- |
| token | JWT 访问令牌 | 24小时（自动续期） |
| refreshToken | 刷新令牌 | 7天 |
| userId | 用户ID | 永久 |
| username | 用户名 | 永久 |

### 6.2 Token 自动刷新（已在 request.js 拦截器实现）

详见 2.3 节的 `request.js` 配置，Token 刷新已集成到 Axios 拦截器中。

### 6.3 页面权限控制

```javascript
// js/auth.js
export function requireAuth() {
  const token = localStorage.getItem('token');
  if (!token) {
    showToast('请先登录', 'warning');
    setTimeout(() => window.location.href = '/login.html', 1000);
    return false;
  }
  return true;
}

export function requireAdmin() {
  const user = appState.getState().user;
  if (!user || user.role !== 'admin') {
    showToast('无权限访问', 'error');
    setTimeout(() => window.location.href = '/index.html', 1000);
    return false;
  }
  return true;
}
```

---

## 7. 移动端适配

### 7.1 响应式布局

使用 Bootstrap 5 的响应式类：

```html
<!-- 导航栏：移动端折叠 -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <!-- 导航链接 -->
  </div>
</nav>
```

### 7.2 移动端优化

| 优化项 | 实现方式 |
| :--- | :--- |
| 触摸优化 | 按钮至少 44x44px，`btn-lg` |
| 图片懒加载 | `loading="lazy"` |
| 下拉刷新 | 可选：使用 Pull-to-refresh 库 |
| 无限滚动 | 替代传统分页，提升移动端体验 |
| 骨架屏 | 加载内容前显示骨架占位 |

---

## 8. 安全注意事项

### 8.1 XSS 防护

后端使用 Jsoup 过滤内容，但前端使用 `marked` 渲染 Markdown 时需额外净化：

```html
<script src="https://cdn.jsdelivr.net/npm/dompurify@3/dist/purify.min.js"></script>
<script>
  // 渲染 Markdown + XSS 净化
  document.getElementById('content').innerHTML =
    DOMPurify.sanitize(marked.parse(markdownContent));
</script>
```

### 8.2 CSRF/XSS 风险提示

JWT 存储在 `localStorage` 中，存在 XSS 攻击被窃取的风险。本项目采用此方案以简化实现，如需更高安全性，可考虑：

- 改为 HttpOnly Cookie 方案（需后端配合）
- 敏感操作增加二次验证

### 8.3 接口限流

系统已集成 Caffeine 本地缓存实现接口限流，防止恶意请求：

| 接口 | 限流策略 |
| :--- | :--- |
| `/user/login` | 同一 IP 每分钟最多 5 次，防止暴力破解 |
| `/user/send-code` | 同一邮箱每 60 秒可发送一次验证码 |
| `/post/{id}/view` | 已登录用户每篇文章每 1 分钟计 1 次，未登录用户基于 IP+UA 每 1 分钟计 1 次 |

### 8.4 CORS 配置

后端已配置跨域支持（Cross-Origin Resource Sharing），允许前端在开发环境下访问 API：

```yaml
# application.yml
cors:
  allowed-origins: http://localhost:3000, http://127.0.0.1:3000
  allowed-methods: GET, POST, PUT, DELETE, OPTIONS
  allowed-headers: Content-Type, Authorization
```

生产环境需根据实际部署域名调整 `allowed-origins`。

---

## 9. 错误码对照表

| code | 后端 message | 前端处理建议 |
| :--- | :--- | :--- |
| 200 | 操作成功 | Toast 提示（可选） |
| 400 | 参数错误 | 显示具体错误信息 |
| 401 | 请先登录 | 跳转登录页 |
| 401 | 无效的刷新Token | 跳转登录页，清除 Token |
| 403 | 无权限 | Toast "无权操作" |
| 403 | 账户已锁定，请在X分钟后重试 | 显示锁定提示 |
| 404 | 资源不存在 | Toast + 跳转 |
| 409 | 数据已存在，操作冲突 | Toast "数据已存在" |
| 500 | 服务器内部错误 | Toast "服务器错误" |

---

## 10. 开发与调试

### 10.1 调试技巧

1. **查看 API 文档**：启动后端后访问 http://localhost:8825/api/doc.html
2. **Network 面板**：查看请求/响应详情
3. **Console 面板**：查看前端日志和错误
4. **LocalStorage**：检查 Token 和用户信息

### 10.2 Mock 登录状态

```javascript
// 在控制台执行
localStorage.setItem('token', 'your-token');
localStorage.setItem('refreshToken', 'your-refresh-token');
localStorage.setItem('userId', '1');
location.reload();
```

---

## 11. API 快速参考表（完整）

### 用户模块 `/user`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /register | ❌ | 注册 |
| POST | /login | ❌ | 登录 |
| POST | /refresh | ❌ | 刷新Token |
| GET | /{id} | ✅ | 获取用户信息 |
| PUT | /password | ✅ | 修改密码 |
| GET | /search | ✅ | 用户搜索 |
| PUT | /profile | ✅ | 更新资料 |
| PUT | /avatar | ✅ | 更新头像 |
| POST | /send-code | ❌ | 发送重置验证码 |
| POST | /reset-password | ❌ | 重置密码 |

### 文章模块 `/post`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | / | ✅ | 发布文章 |
| PUT | /{id} | ✅ | 更新文章 |
| DELETE | /{id} | ✅ | 删除文章 |
| GET | /{id} | ❌ | 文章详情 |
| GET | /list | ❌ | 文章列表 |
| PUT | /{id}/view | ❌ | 增加阅读量 |
| POST | /draft | ✅ | 保存草稿 |
| GET | /draft/latest | ✅ | 最新草稿 |
| DELETE | /draft/{id} | ✅ | 删除草稿 |
| GET | /draft/{id} | ✅ | 获取草稿 |
| GET | /search/advanced | ✅ | 高级搜索 |
| GET | /search/suggest | ❌ | 搜索建议 |
| GET | /my | ✅ | 我的文章 |

### 评论模块 `/comment`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | / | ✅ | 发表评论 |
| GET | /post/{postId} | ❌ | 评论列表 |
| DELETE | /{id} | ✅ | 删除评论 |
| GET | /my | ✅ | 我的评论 |

### 点赞模块 `/like`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /{postId} | ✅ | 点赞/取消 |
| GET | /check/{postId} | ❌ | 检查状态 |
| GET | /my | ✅ | 我的点赞 |

### 收藏模块 `/collect`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /{postId} | ✅ | 收藏/取消 |
| GET | /check/{postId} | ❌ | 检查状态 |
| GET | /my | ✅ | 我的收藏 |

### 关注模块 `/follow`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | / | ✅ | 关注 |
| DELETE | /{userId} | ✅ | 取消关注 |
| GET | /check/{userId} | ❌ | 检查状态 |
| GET | /followers/{userId} | ✅ | 粉丝列表 |
| GET | /following/{userId} | ✅ | 关注列表 |
| GET | /counts/{userId} | ❌ | 数量统计 |

### 通知模块 `/notification`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| GET | /list | ✅ | 通知列表 |
| GET | /unread-count | ✅ | 未读数 |
| PUT | /{id}/read | ✅ | 标记已读 |
| PUT | /read-all | ✅ | 全部已读 |
| DELETE | /{id} | ✅ | 删除通知 |

### 热门模块 `/trending`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| GET | /posts | ❌ | 热门文章 |
| GET | /hot-tags | ❌ | 热门标签 |
| POST | /update/{postId} | ✅ | 更新（管理员） |

### 标签模块 `/tag`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| GET | /list | ❌ | 标签列表 |
| POST | / | ✅ | 创建标签 |

### 校友圈模块 `/circle`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /post | ✅ | 发布动态（支持可见性/评论/转发权限） |
| DELETE | /post/{id} | ✅ | 删除动态 |
| GET | /feed/recommend | ❌ | 推荐流（公开+自己的） |
| GET | /feed/following | ✅ | 关注流（考虑可见性） |
| GET | /post/{id} | ❌ | 动态详情（权限校验） |
| POST | /like/{id} | ✅ | 点赞/取消 |
| GET | /like/check/{id} | ❌ | 检查点赞 |
| GET | /comment/{id} | ❌ | 评论列表（需检查 allowComment） |
| POST | /comment | ✅ | 发表评论（需检查 allowComment） |
| DELETE | /comment/{id} | ✅ | 删除评论 |
| POST | /repost/{id} | ✅ | 转发（需检查 allowRepost） |
| GET | /search | ❌ | 搜索动态（仅公开） |

**可见性规则**：

| visibility | 说明 | 推荐流 | 关注流 | 详情页 |
| :--- | :--- | :--- | :--- | :--- |
| 0 | 公开 | ✅ 所有人 | ✅ 所有人 | ✅ 所有人 |
| 1 | 仅关注者 | ❌ | ✅ 需关注作者 | ✅ 需关注作者 |
| 2 | 仅自己 | ✅ 仅自己 | ❌ | ✅ 仅自己 |

### 媒体模块 `/media`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /upload | ✅ | 上传文件 |
| POST | /upload/multiple | ✅ | 批量上传 |
| DELETE | /{id} | ✅ | 删除文件 |
| GET | /{id} | ✅ | 媒体信息 |
| POST | /bind/{postId} | ✅ | 绑定文章 |
| GET | /post/{postId} | ✅ | 文章媒体 |
| GET | /list | ✅ | 用户媒体 |

### 举报模块 `/report`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | / | ✅ | 提交举报 |
| GET | /my | ✅ | 我的举报 |

### 私信模块 `/message`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | /send | ✅ | 发送私信 |
| GET | /received | ✅ | 收件箱 |
| GET | /sent | ✅ | 发件箱 |
| PUT | /{id}/read | ✅ | 标记已读 |
| DELETE | /{id} | ✅ | 删除私信 |
| GET | /unread-count | ✅ | 未读数 |

### 话题模块 `/topic`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| POST | / | ✅ | 创建话题 |
| GET | /list | ❌ | 话题列表 |
| GET | /hot | ❌ | 热门话题 |
| GET | /{topicId} | ❌ | 话题详情 |
| GET | /{topicId}/posts | ❌ | 话题文章 |

### 管理模块 `/admin`

| 方法 | 路径 | 登录 | 说明 |
| :--- | :--- | :--- | :--- |
| GET | /reports/pending | ✅(admin) | 待处理举报 |
| GET | /reports/{id} | ✅(admin) | 举报详情 |
| PUT | /reports/{id} | ✅(admin) | 处理举报 |
| GET | /user/list | ✅(admin) | 用户列表 |
| PUT | /user/{id}/status | ✅(admin) | 更新用户状态 |
| PUT | /user/{id}/reset-password | ✅(admin) | 重置用户密码 |
| PUT | /user/{id}/ban | ✅(admin) | 封禁用户 |
| GET | /post/list | ✅(admin) | 文章列表 |
| DELETE | /post/{id} | ✅(admin) | 删除文章 |
| GET | /post/review-list | ✅(admin) | 待审核列表 |
| PUT | /post/{id}/approve | ✅(admin) | 审核通过 |
| PUT | /post/{id}/reject | ✅(admin) | 审核拒绝 |
| DELETE | /comment/{id} | ✅(admin) | 删除评论 |
| GET | /statistics/platform | ✅(admin) | 统计数据 |

---

**文档版本**：v1.35
**最后更新**：2026-04-27
